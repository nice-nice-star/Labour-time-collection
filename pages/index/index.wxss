/* index.wxss */
.splash { display:flex; flex-direction:column; align-items:center; justify-content:center; height:100vh; }
.title { font-size:44rpx; font-weight:bold; color:#2b6cb0; }
.sub { margin-top:16rpx; color:#718096; }
